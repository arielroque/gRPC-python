import math
import time

def square_root(x):
    square_root = math.sqrt(x)
    return square_root


def timestamp():
    timestamp = time.time()
    message = f"The timestamp is {timestamp}"
    return message

